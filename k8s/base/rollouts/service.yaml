apiVersion: v1
kind: Service
metadata:
  name: cloudops-store
spec:
  selector: { app: cloudops-store }
  ports:
    - port: 80
      targetPort: 8000
      name: http
---
apiVersion: v1
kind: Service
metadata:
  name: cloudops-store-canary
spec:
  selector: { app: cloudops-store }
  ports:
    - port: 80
      targetPort: 8000
      name: http
