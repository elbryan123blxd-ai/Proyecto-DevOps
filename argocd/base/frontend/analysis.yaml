apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: cloudops-store-canary
  labels:
    app.kubernetes.io/part-of: cloudops-store
spec:
  metrics:
    - name: success-rate
      interval: 30s
      count: 5
      successCondition: result[0] >= 0.95
      failureLimit: 3
      provider:
        prometheus:
          # Activo cuando Fase 5 instale Prometheus (namespace prometheus)
          address: http://prometheus-server.prometheus:9090
          query: |
            sum(
              rate(
                nginx_ingress_controller_requests{
                  ingress="frontend",
                  status!~"5.."
                }[2m]
              )
            )
            /
            sum(
              rate(
                nginx_ingress_controller_requests{
                  ingress="frontend"
                }[2m]
              )
            ) * 100